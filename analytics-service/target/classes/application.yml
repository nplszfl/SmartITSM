spring:
  application:
    name: analytics-service
  datasource:
    url: jdbc:mysql://localhost:3306/smartitsm?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: 8084

logging:
  level:
    com.smartitsm.analytics: DEBUG
