server:
  port: 8015
spring:
  application:
    name: knowledge-service
  datasource:
    url: jdbc:mysql://localhost:3306/smartitsm_knowledge?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root123
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-type: mysql